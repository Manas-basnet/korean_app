import 'package:flutter/material.dart';

class BooksShell extends StatelessWidget {
  final Widget child;

  const BooksShell({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}

class TestsShell extends StatelessWidget {
  final Widget child;

  const TestsShell({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}

class ProfileShell extends StatelessWidget {
  final Widget child;

  const ProfileShell({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}